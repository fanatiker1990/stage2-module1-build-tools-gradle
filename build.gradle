repositories {
    mavenCentral()
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'maven-publish'
    dependencies {
        testImplementation('org.junit.jupiter:junit-jupiter:5.8.1')
    }
}